# AGENTS.MD

## Task Context

We are participating in the SomosNLP 2026 hackathon with the project **RosettIA - Quechua**.

The immediate task that created this file was to establish a durable working memory for current and future agent sessions: store useful findings, caveats, project intent, and operational notes that may matter later.

## Hackathon Context

- Event: **#HACKATHONSomosNLP 2026**, 5th edition.
- Main theme: post-training of LLMs.
- Suggested technical directions from organizers:
  - Reinforcement learning.
  - Preference alignment.
  - Test-time learning.
  - DPO/alignment using generated preference data.
- Cultural direction: projects are open, but organizers encourage links to cultures of LATAM and the Iberian Peninsula.
- Launch date from announcement: **Monday, May 11, 2026**.
- Workshop: Sergio Paniego, MLE at Hugging Face, on post-training techniques and libraries.
- Main info page: https://somosnlp.org/hackathon
- Hugging Face org invite from announcement: https://huggingface.co/organizations/somosnlp-hackathon-2026/share/DNcqoZrtSmEkyLLOiSYTQCzkcrquceDoVY
- Registration link from announcement: https://hackathon-somosnlp-2026.eventbrite.com/
- Workshop recording playlist from announcement: https://www.youtube.com/playlist?list=PLTA-KAy8nxaCGGYz5CWiLZNzc31ilPDyI
- Book/resource mentioned in workshop: https://huggingface.co/spaces/nanotron/ultrascale-playbook

## Project: RosettIA - Quechua

Core idea from the team thread:

> Build open-source translation technology for low-resource languages, starting with Quechua, the most widely spoken Indigenous language in the Americas. The long-term goal is to adapt the techniques to additional languages and reduce the technological lag affecting speakers of these languages.

Motivation:

- Low-resource translation remains underdeveloped despite rapid progress in LLMs.
- Quechua is a high-impact starting point because of its speaker population and cultural importance.
- The project should support language survival and inclusion for people who primarily or only speak Indigenous languages.
- Keep the work open-source and reusable for future languages where possible.

Known team members from the project announcement:

- Thermostatic
- NiiaTheBugg

Current language scope:

- Default target variety for now: **Chanka Quechua**.
- Reason: the team currently considers this the safest path because of licensing concerns around other available Quechua resources.
- Future agents should not broaden the scope to all Quechua varieties unless the team explicitly approves it and the relevant dataset/model licenses have been checked.

## Current Repository State

As of 2026-05-18, the repo is a Python `>=3.12` project managed with `uv`.

Current structure:

- `data/raw/`: local source files such as PDFs. Ignored by git.
- `data/interim/`: local extraction artifacts. Ignored by git.
- `data/processed/`: local generated datasets. Ignored by git.
- `docs/references/`: source notes, license notes, and extraction caveats.
- `scripts/`: reproducible data preparation scripts.
- `src/rosettia/`: importable project code.
- `tests/`: test suite placeholder.

Important git convention:

- Do **not** commit data artifacts: PDFs, extracted text, CSV, Parquet, generated datasets, model files, or other large/local data. `data/**` is ignored except `.gitkeep` placeholders.
- Public processed dataset release: https://huggingface.co/datasets/Thermostatic/rosettia-chanka-data
- The HF dataset release intentionally does not include the source PDF. It includes processed Parquet artifacts plus metadata only.

## Working Principles For Future Agents

- Preserve user/team intent: this is a hackathon project about Quechua translation and low-resource language inclusion, not a generic translation demo.
- Prefer small, well-documented steps that can survive handoff between sessions.
- Record important findings here when they affect future work: datasets, licensing constraints, model choices, evaluation pitfalls, commands, credentials setup expectations, and known blockers.
- Avoid inventing facts about Quechua varieties, datasets, or licensing. Verify with primary sources or dataset cards when possible.
- Treat Quechua as a language family/continuum with regional varieties. Be explicit about which variety or dataset a model/evaluation targets.
- For now, assume Chanka Quechua is the intended target variety and flag any resource that mixes varieties or does not clearly identify the variety.
- Be careful with Spanish-centric assumptions. Quechua translation may involve Spanish, English, or other pivot languages, but the project goal is not just Spanish NLP.
- Favor reproducible pipelines over one-off notebooks when practical.
- Prefer modern libraries and columnar data tooling. Use **Polars** for tabular dataset work unless there is a strong reason not to.
- Keep licensing and consent visible, especially for Indigenous-language data.
- When using Hugging Face datasets/models, read model cards and dataset cards before integrating them.
- When adding training or post-training code, include a minimal way to run a smoke test locally.

## Likely Technical Directions To Explore

These are candidate directions, not commitments:

- Baseline translation model or pipeline for Quechua <-> Spanish and/or Quechua <-> English.
- SFT data collection or conversion from public parallel corpora.
- Preference dataset construction for translation quality, cultural appropriateness, and dialect/variant sensitivity.
- DPO or another preference-alignment method on a small open model if GPU credits are available.
- RLVR-style reward design if a reliable automatic verifier or evaluator can be built.
- Synthetic data generation with careful human review or filtering.
- Evaluation with automatic metrics plus qualitative review by humans familiar with the target variety.

## Caveats

- The hackathon organizers mentioned GPU credits after contributing to the preference dataset. Future agents should confirm the current process before assuming access.
- Announcements and Discord channel names came from user-provided text, not direct live verification.
- Current source PDF: `data/raw/source_documents/manual_quechua_chanka_administracion_justicia_2014.pdf`.
- Current source note: `docs/references/manual_quechua_chanka_administracion_justicia_2014.md`.
- Current extraction script: `scripts/extract_manual_parallel_corpus.py`.
- Current extraction result: 1,012 draft Spanish-Chanka Quechua candidates from pages 17-118, of which 800 auto-pass table extraction checks and 212 are flagged for review. Generated files are local and git-ignored.
- Current reviewed result: 1,011 included rows and 974 training-ready pairs. One row from page 104 is excluded because visual review showed a semantic mismatch between Spanish and Quechua cells. Rows with slash alternatives are kept in the reviewed artifact but excluded from the training-ready subset until split/normalized.
- Current split-alternative result: 37 slash-alternative rows split into 81 clean rows. Augmented training-ready corpus has 1,055 pairs and no slash markers.
- Current glossary result: 423 terminology entries from the vocabulary sections, including 219 simple term pairs. Keep glossary artifacts separate from sentence-pair training unless a deliberate formatting strategy is chosen.
- Current external-source research note: `docs/source_inventory_chanka.md`.
- Current broad dirty SFT source note: `docs/references/somosnlp_spanish_to_quechua_dirty.md`.
- Current broad dirty SFT preprocessing script: `scripts/preprocess_somosnlp_dirty_parallel.py`.
- The SomosNLP 2022 Spanish-to-Quechua Hugging Face dataset should be treated as broad, noisy Quechua translation data only. It is not Chanka-verified, and the visible dataset card did not expose a clear license during initial review. Keep its outputs in a separate tier from reviewed Chanka data.
- Current SomosNLP dirty preprocessing run: 128,434 raw rows, 123,839 unique normalized pairs, 82,873 strict high-quality SFT rows, and 111,699 broad backup SFT rows. Generated outputs are git-ignored under `data/`.
- Current AmericasNLP source note: `docs/references/americasnlp_quechua_spanish.md`.
- Current AmericasNLP preprocessing script: `scripts/preprocess_americasnlp_quechua_spanish.py`.
- AmericasNLP 2024 Quechua-Spanish can be used as another broad adaptation tier with `quy` data, but it is not reviewed Chanka judicial-domain data. Keep synthetic/backtranslated data opt-in only.
- Current AmericasNLP real-source preprocessing run: 142,116 raw aligned rows, 135,566 unique normalized pairs, 87,004 high-quality real SFT rows, and 996 eval rows. Generated outputs are git-ignored under `data/`. Synthetic/backtranslated data is opt-in only via `--include-synthetic`.
- Current broader data-source research note: `docs/references/other_parallel_data_candidates.md`. Best next candidates are FLORES `spa_Latn`/`quy_Latn` for evaluation, PanLex Chanka/Ayacucho CC0 lexical data for terminology/reward features, and IWSLT only for restricted experiments because of NC/ND licensing.
- Current HF dataset release: `Thermostatic/rosettia-chanka-data`, public. Uploaded files include clean Chanka reviewed/training/glossary Parquets and broad Quechua filtered/scored Parquets. No PDF was uploaded.
- Current SFT training script: `scripts/train_sft_unsloth.py`.
- Current SFT training note: `docs/unsloth_sft.md`.
- Unsloth official Qwen3.5 docs say QLoRA/4-bit training is not recommended for Qwen3.5. Use 16-bit LoRA with `load_in_4bit=False`, `load_in_16bit=True`, `use_gradient_checkpointing="unsloth"`, and validation enabled. The script creates eval splits, selects best checkpoints by `eval_loss`, and can continue Chanka SFT from the broad LoRA with `--adapter-path`.
- Remote SFT setup on `root@91.224.44.81 -p 20899`: repo path `/root/rosettia-chanka`, venv `.venv`, GPU `NVIDIA GeForce RTX 3090` with Torch `2.10.0+cu128`, Unsloth `2026.5.5`, Polars `1.40.1`. Smoke test passed on 2026-05-20 with `--stage broad --max-train-samples 32 --max-eval-samples 8 --max-steps 2 --eval-steps 1 --save-steps 1 --output-dir outputs/smoke`; validation ran during training, checkpoints were saved, and `outputs/smoke/broad/final_lora` was produced. Flash Attention 2 was not installed, so Unsloth used XFormers fallback.
- GitHub issue #2 requested creating `brancheef` in the owner repo from `niiaespinosa/rosettia-chanka:brancheef`. The branch was created, then merged into `main` after the user clarified they wanted it merged too.
- Current GSPO paper-profile plan: `docs/findings/gspo_paper_profile_plan.md`. Independent scripts live under `experiments/gspo/`: `run_2411_fine_grained_severity.sh`, `run_2310_severity_proxy.sh`, `run_2511_self_verifier.sh`, `run_2511_vibethinker.sh`, `run_2511_train_verifier_then_gspo.sh`, and `run_2511_learned_verifier_gspo.sh`.
- Do not present the 2310 severity-proxy run as real xCOMET. We do not have a Chanka-capable xCOMET model. Treat it only as a low-cost transparent severity ablation.
- DeepSeek-style verifier work is now the serious reward-model path. `scripts/train_verifier_chanka_unsloth.py` trains a separate JSON-scoring verifier LoRA from clean Chanka pairs plus synthetic corruptions, with validation enabled. The verifier data now includes easy corruptions plus harder fluent Chanka negatives: unrelated Chanka translations, mixed-reference candidates, unsupported Chanka additions, and repetition damage. `learned_verifier_2511` in `scripts/train_gspo_chanka_unsloth.py` can load that verifier LoRA in the reward loop and blend its JSON score with hard anti-copy/leakage/reference guards. This is expensive but now preferred over leaning on the severity proxy.
- DeepSeekMath-V2 self-verification has been adapted to translation in `docs/findings/deepseekmath_v2_translation_adaptation.md`. `scripts/train_gspo_chanka_unsloth.py` now has reward profile `self_verifiable_translation_2511`, which asks the translator to emit `Traduccion final`, `Autoevaluacion`, and `Puntaje: \boxed{...}`; it rewards final-translation quality plus calibrated self-assessment with the paper-inspired `0.76/0.24` weighting, then strips the analysis before corpus metrics. The intended "thinking" signal is the explicit, rewardable `Autoevaluacion`, not an uncontrolled open `<think>` trace. The script patches Qwen chat templates for GSPO because TRL's internal prompt renderer otherwise leaves an open `<think>` tag and completions clip on reasoning traces. `scripts/build_self_verifiable_translation_data.py` builds cold-start verifier, meta-verifier, and self-verifying generator JSONL from clean Chanka pairs. `scripts/train_meta_verifier_chanka_unsloth.py` trains the meta-verifier, and `self_verifiable_translation_2511` can use it via `--meta-verifier-adapter-path`. `experiments/gspo/run_2511_train_meta_verifier_then_self_gspo.sh` chains cold-start data -> meta-verifier SFT -> self-verifiable GSPO. Current caveat: cold-start meta-verifier rows are synthetic; the true DeepSeekMath loop still needs hard self-analysis failures mined from real model rollouts.
- Bounded translation-thinking is now a separate canary profile, not a raw Qwen `<think>` run. `self_verifiable_thinking_translation_2511` asks for `Analisis de traduccion`, `Traduccion final`, `Autoevaluacion`, and boxed `Puntaje`; the reward requires the thinking field, scores it for concise translation-specific checks, and still strips everything except `Traduccion final` for corpus metrics. The thinking prompt is intentionally tight: 1-2 checks, maximum 35 words, no step-by-step rationale; the launcher default completion cap is 112 tokens after the first canary showed ~100-token analyses and 25% clipping at 144. Launch with `experiments/gspo/run_2511_self_verifiable_thinking_translation.sh`, preferably with the current meta-verifier v2 via `META_VERIFIER_ADAPTER=outputs/chanka_translation_meta_verifier_v2_20260523-meta-v2-self/final_meta_verifier_lora`. This is the right way to test the user's "DeepSeekMath needs thinking" hypothesis without reintroducing uncontrolled chat-template reasoning traces.
- Bounded-thinking mining is wired as the next DeepSeekMath loop. `scripts/evaluate_gspo_checkpoint.py --self-verification-thinking-output` prompts for the `Analisis de traduccion` format and still writes parsed `self_verification`; `experiments/gspo/queue_meta_verifier_v3_from_thinking_outputs.sh` waits for a bounded-thinking adapter, mines those outputs, trains meta-verifier v3 on cold-start + real thinking failures, and launches another bounded-thinking GSPO run with that v3 meta-verifier.
- `queue_meta_verifier_v3_from_thinking_outputs.sh` now defaults `MINE_BATCH_SIZE=4`; the first restarted v3 mining job accidentally used batch 1 and had only reached 32/320 predictions after a long wait. Batch 4 matches the already-tested batched final evaluator on the L40S and should be preferred unless VRAM says otherwise.
- Bounded-thinking cold-start SFT is now wired too. `scripts/build_self_verifiable_translation_data.py` writes `self_verifiable_thinking_generator_sft.jsonl`; `scripts/train_jsonl_sft_unsloth.py --prompt-self-verification-thinking --target-field target` can teach the exact `Analisis de traduccion`/`Traduccion final`/`Autoevaluacion`/boxed-score format before RL. `experiments/gspo/run_2511_train_thinking_generator_then_gspo.sh` chains cold-start data -> bounded-thinking generator SFT -> bounded-thinking GSPO with the meta-verifier. This is closer to the DeepSeekMath pattern than going straight from normal SFT into reasoning-format RL.
- Translation thinking now uses explicit primitive tags, not generic reflection. The prompt/cold-start target asks for short `Analisis de traduccion` spans with tags like `[SIGNIFICADO]`, `[GRAMATICA]`, `[ENTIDADES]`, `[TERMINOLOGIA]`, and `[ANTI_COPIA]`; `translation_thinking_score()` rewards concise analyses that hit multiple primitive categories and `self_verification_diagnostics()` reports average primitive count. This is the current "DeepSeekMath for language" hypothesis: make grammar/meaning checks discrete and rewardable, then let GSPO optimize calibrated use of those primitives.
- The SFT-seeded thinking runner now prefers meta-verifier v3 when `outputs/chanka_translation_meta_verifier_v3_thinking_20260523-meta-v3-thinking/final_meta_verifier_lora` exists, falling back to v2 only if v3 is absent. The waiting queue uses a smaller 32-row GSPO eval and evaluates only at the final 8-step canary to avoid spending most of the experiment on slow meta-verifier evaluation.
- Use `experiments/gspo/queue_2511_thinking_generator_sft_after_active.sh` to queue the SFT-seeded thinking branch behind active mining/training/eval jobs. Its canary defaults are source rows 256, SFT 32 steps, GSPO 8 steps, 256/32 GSPO train/eval rows, with v3 meta-verifier if available. This is the preferred next branch after the negative normal-adapter bounded-thinking canary.
- First tightened bounded-thinking canary completed on the remote L40S: `outputs/gspo_paper_profiles/2511_self_verifiable_thinking_translation_20260523-thinking-meta-v2/chanka_gspo/final_metrics.json`. It fixed the token explosion but not the translation problem. Final metrics on the capped 64-row eval: chrF++ 35.1158, BLEU 12.9443, TER 95.3757, token F1 18.2616, trainer eval reward 0.0911. Diagnostics: format/thinking-format 45.3125%, missing score 54.6875%, avg self-score 0.9552 vs true score 0.2577, false-confidence 93.1034%, avg thinking score 0.1742. Interpretation: bounded `Analisis de traduccion` is mechanically viable, but a normal translator adapter does not reliably learn the reasoning format from 8 GSPO steps. Prefer the new thinking-generator SFT seed before scaling this path.
- Meta-verifier v3 mining from that failed bounded-thinking adapter completed on 2026-05-23: `outputs/self_verification_mining/20260523-meta-v3-thinking/`. Metrics over 320 rows: chrF++ 39.4663, BLEU 9.0469, token F1 28.3023, TER 105.8957, format/thinking-format 86.875%, missing score 13.125%, avg self-score 0.9586 vs true score 0.3278, false-confidence 91.7266%. Real meta rows: 317 records, 254 `false_confidence_self_score_too_high`, 40 `missing_self_score`, 22 `self_analysis_matches_translation_quality`, 1 underconfident; severities 269 critical, 26 major, 18 minor, 4 none. This is exactly the kind of hard self-analysis failure data needed for the DeepSeekMath-style meta-verifier loop.
- Use `experiments/gspo/queue_2511_self_verifiable_thinking_after_current_gspo.sh` to launch the first bounded-thinking canary after any active GSPO process exits. Ad hoc SSH one-liners proved fragile; the durable queue script waits on `scripts/train_gspo_chanka_unsloth.py`, then calls `run_2511_self_verifiable_thinking_translation.sh` with meta-verifier v2 by default.
- Self-verification diagnostics are now emitted by structured-output eval and final GSPO metrics. Watch `self_verification_format_rate`, `self_verification_required_format_rate`, `self_verification_missing_score_rate`, `self_verification_avg_score_gap`, `self_verification_false_confidence_rate`, and `self_verification_avg_thinking_score` before trusting any reward improvement. A true DeepSeekMath-language loop needs calibration and useful analysis traces, not just better BLEU/chrF.
- Final GSPO held-out generation in `scripts/train_gspo_chanka_unsloth.py` is batched with `--per-device-eval-batch-size`. Earlier self-verifiable/meta-verifier runs spent a long time after training because `generate_predictions()` decoded the 158-row eval set one row at a time. Queued bounded-thinking runs should use the batched path; already-running Python processes will not pick it up until restarted.
- Self-verifiable GSPO launchers now accept `MAX_TRAIN_SAMPLES` and `MAX_EVAL_SAMPLES`. The bounded-thinking queue and meta-verifier v2/v3 queue scripts default GSPO canaries to `256` train rows and `64` eval rows to keep the DeepSeekMath loop moving. Override these for a fuller contender only after the format/calibration diagnostics look sane.
- Structured-output parsing now strips trailing `Autoevaluacion`/`Puntaje` even when the model omits the `Traduccion final:` marker. The first meta-v2 self-verification eval showed many outputs like `¿Ima kallpim tiyanki? Autoevaluacion: ... Puntaje: \boxed{1.0}`; scoring the suffix as translation distorted metrics and verifier labels. The deeper failure remains false confidence and weak format following, but the parser should no longer punish partially structured outputs by including the self-eval text in the translation.
- Recovered meta-v2 self-verification eval from `outputs/gspo_paper_profiles/2511_self_verifiable_translation_meta_v2_20260523-meta-v2-self/chanka_gspo/final_gspo_lora` was poor: chrF++ `38.8286`, BLEU `5.7896`, TER `138.2488`, token F1 `23.2703`, format rate `46.20%`, missing self-score `53.80%`, avg self-score `1.0`, avg true score `0.2900`, avg score gap `0.7687`, false-confidence rate `97.26%`. This confirms meta-verifier v2 did not produce a useful self-verifying translator; bounded-thinking/meta-verifier-v3 is the right next canary.
- Real self-analysis mining is now implemented for meta-verifier v2. `scripts/evaluate_gspo_checkpoint.py --self-verification-output` writes raw structured outputs plus parsed `self_verification`; metrics still score only `Traduccion final`. `scripts/build_meta_verifier_from_self_outputs.py` labels the model's own self-analyses by comparing boxed self-score to hidden-reference translation quality, catching false confidence, missing scores, underconfidence/hallucinated issues, and calibrated analyses. `experiments/gspo/queue_meta_verifier_v2_from_self_outputs.sh` waits for a self-verifiable adapter, mines real self outputs, trains meta-verifier v2 on cold-start + real rows, then runs self-verifiable GSPO with the v2 meta-verifier. This is the first actual rollout-to-meta-verifier iteration and should be preferred over repeatedly training only on synthetic meta rows.
- The verifier trainer can now take real model outputs via repeated `--candidate-jsonl` flags. Use the prediction JSONL files from `scripts/evaluate_gspo_checkpoint.py` as hard negatives/near-misses for a stronger second-pass verifier if the current hard-negative verifier is not discriminative enough.
- Do not optimize serious GSPO selection against the xCOMET-inspired severity proxy. We do not expect to have a real Chanka-capable xCOMET evaluator, so `severity_proxy_2310` should stay a transparent ablation/diagnostic only. Serious reward-model work should prioritize the learned hard-negative verifier and external lexical sanity metrics.
- Current evaluation stance: cheap external metrics and transparent proxies are still useful for regression triage, but they are not the product objective and should not be treated as a real Chanka quality judge. Use chrF++/BLEU/token-F1/copy/leakage/TER to catch obvious failures and compare canaries, then use the learned hard-negative verifier plus qualitative review for serious GSPO selection. If proxies and qualitative samples disagree, trust the samples and train a stronger verifier rather than chasing the proxy.
- Current xCOMET stance: assume we will not have a usable Chanka-capable xCOMET evaluator. xCOMET-inspired/severity proxies still make sense as cheap filters and failure accounting, but not as evidence that the translator is good. For serious model selection, prioritize reload-generation quality, clean held-out references, artifact/copy/leakage metrics, qualitative samples, and a DeepSeek-style trained verifier. If cost is not the constraint, train stronger verifier passes from real candidate outputs and pairwise/listwise preference labels rather than optimizing harder against the proxy.
- Remote smoke on `root@216.81.248.197 -p 20299` passed for the renamed severity proxy, repaired VibeThinker batching, and chained verifier+GSPO script. VibeThinker needs per-device `TRAIN_BATCH_SIZE=8` when `NUM_GENERATIONS=8`; the previous `TRAIN_BATCH_SIZE=2` crashed in eval reward reshaping.
- Current GSPO canary sweep script: `experiments/gspo/run_canary_sweep.sh`. It tests individual paper profiles plus mixed profiles `mix_severity_verifier`, `mix_verifier_vibe`, `mix_all_strict`, `rosettia_guard_v1`, and `rosettia_guard_v2`, then summarizes metrics with `scripts/summarize_gspo_canaries.py`.
- Current full-contender launcher: `experiments/gspo/run_best_canary_full.sh`. Set `SWEEP_DIR` to a completed canary sweep; it selects the top external-metric `summary.jsonl` profile unless `PROFILE` is overridden, then runs a two-epoch GSPO contender over the full clean Chanka corpus with validation/checkpoints.
- Live GSPO status on `root@216.81.248.197 -p 20299` as of 2026-05-21 08:42 UTC:
  - Canary sweep `outputs/gspo_canary_sweeps/20260521-paper-mix-v1` completed. Top external-metric profile was `vibethinker_2511` with selection `21.0148`, chrF++ `37.0672`, BLEU `6.4582`, token F1 `18.2594`, source-copy `4.0551%`, exact-copy `1.5625%`, and leakage `0.3906%`.
  - First full VibeThinker contender reached `outputs/gspo_full_contenders/vibethinker_2511_20260521-073724/chanka_gspo/checkpoint-196`; eval reward there was `0.30345`. Earlier checkpoints: `checkpoint-28` eval reward `0.30127`, `checkpoint-56` eval reward `0.29794`, `checkpoint-112` eval reward `0.30599`. That original process later stopped using GPU SM while still holding GPU memory and was killed; it had been launched before completion-table logging was disabled.
  - Replacement VibeThinker contender at `outputs/gspo_full_contenders/vibethinker_2511_continued_no_tables_20260521-083900` was initialized from the old GSPO `checkpoint-196`, with `--no-log-completions` and `MAX_STEPS=1598`. It produced `checkpoint-28` eval reward `0.31191`, `checkpoint-56` eval reward `0.30850`, `checkpoint-84` eval reward `0.30904`, and `checkpoint-112` eval reward `0.31448`.
  - True `--resume-from-checkpoint` worked technically but TRL preserved the old 28-step eval/save cadence from the checkpoint despite new `EVAL_STEPS=112`/`SAVE_STEPS=112`. To actually reduce validation-generation overhead, Vibe phase 2 was launched fresh from adapter `outputs/gspo_full_contenders/vibethinker_2511_continued_no_tables_20260521-083900/chanka_gspo/checkpoint-112` into `outputs/gspo_full_contenders/vibethinker_2511_phase2_from112_eval112_20260521-0909` with `MAX_STEPS=1486`, `EVAL_STEPS=112`, `SAVE_STEPS=112`, `WARMUP_RATIO=0.01`, and `--no-log-completions`.
  - Easy learned-verifier SFT was stopped early at `outputs/chanka_translation_verifier_full_r128/checkpoint-116` because validation loss was already `0.00244` on easy synthetic labels and more epochs were wasteful. Do not confuse this with a failed run; it was intentionally stopped after a good checkpoint.
  - Attempted learned-verifier GSPO at `outputs/gspo_paper_profiles/2511_learned_verifier_full_early116`; it failed on the first reward call because the verifier processor treated prompts as images. Commit `2f0f725` fixed this by passing `text=batch_prompts` explicitly. That failed output dir has no final metrics.
  - Hard-negative verifier SFT is running in parallel at `outputs/chanka_translation_verifier_hard_r128` to use idle GPU capacity; it has 8,674 verifier examples and validation every 57 steps. Latest checked checkpoint was `checkpoint-1083`; best eval loss remained `0.002326` from `checkpoint-912`, current eval loss was `0.002389`.
  - Hard-negative learned-verifier GSPO queue is alive via `outputs/queue_hard_learned_verifier_after_fulls.sh`; it waits until no `train_gspo_chanka_unsloth.py`/`train_verifier_chanka_unsloth.py` processes remain and at least one full-contender metrics file exists.
- Live GSPO status on `root@216.81.248.197 -p 20299` as of 2026-05-21 10:17 UTC:
  - Hard-negative verifier SFT reached `outputs/chanka_translation_verifier_hard_r128/checkpoint-1383` but crashed during final save with CUDA illegal memory access. Best checkpoint is `checkpoint-1368` with eval loss `0.0022854567505419254`; `checkpoint-1383` eval loss was `0.002302516484633088`. Remote symlink `outputs/chanka_translation_verifier_hard_r128/final_verifier_lora -> checkpoint-1368` was created for scripts that expect a final adapter path.
  - Broad-SFT learned-verifier canary `outputs/gspo_paper_profiles/2511_learned_verifier_hard_canary_20260521-0936` did not beat Vibe externally: chrF++ `35.77395`, BLEU `6.19078`, token F1 `17.04413`, source-copy `4.0551%`, exact-copy `1.5625%`, leakage `0.3906%`, TER `102.89017`, trainer eval reward `0.23386`.
  - Learned-verifier-on-Vibe canary `outputs/gspo_paper_profiles/2511_learned_verifier_on_vibe112_canary_20260521-1000` is the current best canary by external metrics: chrF++ `38.20628`, BLEU `7.28880`, token F1 `18.97321`, source-copy `4.1667%`, exact-copy `1.5625%`, leakage `0.3906%`, TER `96.53179`, trainer eval reward `0.24287`. It starts from Vibe `checkpoint-112` and uses the hard verifier `checkpoint-1368`.
  - `scripts/evaluate_gspo_checkpoint.py` evaluates arbitrary saved GSPO checkpoints on the clean Chanka validation split and writes external metrics/predictions. Use it for checkpoint selection because final-step metrics may miss an earlier stronger checkpoint.
  - `learned_verifier_vibe_2511` is available as a new mix profile: it uses the hard-negative verifier reward plus the VibeThinker group diversity bonus. Use `experiments/gspo/queue_learned_verifier_vibe_canaries.sh` after the current full contenders finish; it first evaluates selected full-run checkpoints externally, then runs 4-generation and 8-generation canaries. It requires `--verifier-adapter-path`.
  - Full learned-verifier-on-Vibe contender is running at `outputs/gspo_full_contenders/learned_verifier_on_vibe112_full_20260521-1016` from Vibe `checkpoint-112`, with hard verifier `checkpoint-1368`, `MAX_STEPS=224`, `EVAL_STEPS=56`, `SAVE_STEPS=56`, `LR=7e-7`, `NUM_GENERATIONS=4`, verifier batch size `2`, and no completion-table logging.
  - Full learned-verifier-on-Vibe checkpoints: `checkpoint-56` eval reward `0.25755`, eval loss `0.02497`, eval runtime `762.2s`; `checkpoint-112` eval reward `0.26144`, eval loss `0.04412`, eval runtime `768.6s`; `checkpoint-168` eval reward `0.25736`, eval loss `0.02710`, eval runtime `717.1s`. Checkpoint `112` remains the best learned-verifier full checkpoint so far and was preserved on the remote at `outputs/gspo_selected_checkpoints/learned_verifier_on_vibe112_checkpoint_112`.
  - Full learned-verifier-on-Vibe finished at `checkpoint-224` and wrote final metrics: chrF++ `37.84528`, BLEU `5.64736`, token F1 `21.86668`, source-copy `2.4367%`, exact-source-copy `0.6329%`, leakage `0.6329%`, TER `94.0092`, trainer eval reward `0.26116`. Checkpoint `224` was also preserved on the remote at `outputs/gspo_selected_checkpoints/learned_verifier_on_vibe112_checkpoint_224` because its eval loss `0.01096` was much better than checkpoint `112` even though its reward was slightly lower.
  - Vibe phase 2 continuation was stopped after sustained decline. It peaked at `checkpoint-896` with eval reward `0.33324`, then dropped to `0.33250` at `checkpoint-1008`, `0.32695` at `checkpoint-1120`, and `0.32441` at `checkpoint-1232`. A copy of `checkpoint-896` was preserved on the remote at `outputs/gspo_selected_checkpoints/vibethinker_phase2_checkpoint_896` before save rotation could delete it. Reward scales are profile-specific; final external metrics matter most.
  - A live external evaluation attempt of preserved checkpoints was stopped because it competed with the learned-verifier full run and had not written metrics yet. Let the queued post-training checkpoint evaluation run after `train_gspo_chanka_unsloth.py` exits, when it will not compete with the full contender.
  - Ignore checkpoint eval directory `outputs/gspo_checkpoint_evals/20260521-114759`: it used a batched evaluator before decoder-only left padding was forced and produced obviously invalid low metrics for the preserved Vibe checkpoint. Commit `30d339f` fixes the evaluator by setting `tokenizer.padding_side = "left"` before batched generation. Current clean queue stamp is `20260521-115135`.
  - Saved GSPO checkpoint reloads initially appeared to show a severe chat-template artifact failure: predictions repeated `user`, `assistant`, and `<think>` after a plausible short translation. Commit `3e6e01f` still adds useful `chat_artifact_penalty` reward guards and corpus metrics, but the main reload failure was later traced to an EOS mismatch: the processor/tokenizer EOS was `<|im_end|>` id `248046`, while the loaded model generation config used EOS id `248044`. Always pass `eos_token_id=tokenizer.eos_token_id` and align `model.generation_config.eos_token_id`/`pad_token_id` after loading Unsloth Qwen3.5 adapters.
  - Focused artifact follow-up queue `outputs/queue_artifact_penalty_followup_20260521-115915b.log` supersedes the broader queue. It evaluated learned-verifier checkpoint `168`, preserved checkpoint `224`, and `final_gspo_lora` as reloads. All failed with chrF++ around `20.2`, BLEU around `0.44`, TER around `676`, and `chat_artifact_penalty` around `58`. The re-summarized checkpoint eval dir is `outputs/gspo_checkpoint_evals/20260521-115915b`; selection is around `-30.8` after artifact penalty. The same queue then starts artifact-penalized `learned_verifier_vibe_2511` 4-gen and 8-gen canaries.
  - Artifact-penalized `learned_verifier_vibe_2511` 4-gen canary `outputs/gspo_paper_profiles/2511_artifact_guard_learned_verifier_vibe_4gen_canary_20260521-115915b` finished with in-memory final metrics: chrF++ `38.19053`, BLEU `7.22303`, token F1 `19.49405`, source-copy ratio `4.1667%`, exact-source-copy `1.5625%`, Spanish leakage `0.390625%`, chat artifact penalty `0.0`, TER `97.10983`, trainer eval reward `0.24584`. After the EOS fix, raw saved-adapter reload with `--batch-size 1` produced chrF++ `37.99378`, BLEU `8.22530`, token F1 `22.55735`, source-copy ratio `2.9114%`, exact-source-copy `0.6329%`, Spanish leakage `0.7911%`, chat artifact penalty `0.0`, and TER `92.62673` over 158 eval rows. This is now a serious contender.
  - Artifact-penalized `learned_verifier_vibe_2511` 8-gen canary started after the 4-gen canary at `outputs/gspo_paper_profiles/2511_artifact_guard_learned_verifier_vibe_8gen_canary_20260521-115915b`, but was stopped early after checkpoint `16` because eval reward stayed below the 4-gen run: checkpoint `8` reward `0.23739`, checkpoint `16` reward `0.23563`. Use 4-gen for the next contender unless a later hypothesis specifically justifies paying for 8-gen again.
  - EOS-fixed checkpoint ranking dir: `outputs/gspo_checkpoint_evals/20260521-eosfix-ranking`. Current best saved adapter is `learned_verifier_vibe_on_vibe896_4gen_canary_b1`, from canary `outputs/gspo_paper_profiles/2511_learned_verifier_vibe_on_vibe896_4gen_canary_20260521-133146`, with selection `26.3808`, chrF++ `40.9703`, BLEU `8.1555`, token F1 `26.6169`, source-copy `2.8270%`, exact-copy `0.6329%`, leakage `0.6329%`, artifact `0.0%`, and TER `88.7097`. Second place is raw `vibethinker_phase2_checkpoint_896` with selection `25.6181`, chrF++ `40.2643`, BLEU `5.8940`, token F1 `25.8484`, source-copy `2.5105%`, exact-copy `0.6329%`, leakage `0.7911%`, artifact `0.0%`, and TER `88.7097`.
- Full serious contender now running on the remote: `outputs/gspo_full_contenders/learned_verifier_vibe_on_vibe896_full_20260521-135419`, log `outputs/learned_verifier_vibe_on_vibe896_full_20260521-135419.log`, PID observed as `122513`. It starts from `outputs/gspo_selected_checkpoints/vibethinker_phase2_checkpoint_896`, uses reward `learned_verifier_vibe_2511`, verifier `outputs/chanka_translation_verifier_hard_r128/checkpoint-1368`, `MAX_STEPS=224`, `EVAL_STEPS=56`, `SAVE_STEPS=56`, LR `7e-7`, warmup ratio `0.01`, `NUM_GENERATIONS=4`, train/eval batch `4`, GA `2`, and full clean Chanka split of 897 train / 158 validation rows.
- Same full serious contender reached and preserved `checkpoint-168` at `outputs/gspo_selected_checkpoints/learned_verifier_vibe_on_vibe896_checkpoint_168`. Trainer eval reward was `0.27686325638684667`, eval loss `0.01147084217518568`. This is lower than checkpoint `112` reward `0.2869656496221506`; wait for batch-size-1 external checkpoint eval before choosing among 56/112/168/224/final.
- The full serious contender finished on 2026-05-21 15:15 UTC and preserved `checkpoint-224` at `outputs/gspo_selected_checkpoints/learned_verifier_vibe_on_vibe896_checkpoint_224`. In-memory final metrics: chrF++ `40.12930518900551`, BLEU `6.0075693712342275`, token F1 `25.644966847498495`, source-copy ratio `2.668776371308017%`, exact-copy `0.6329113924050633%`, Spanish leakage `0.7911392405063291%`, chat artifact penalty `0.0`, TER `89.86175115207374`, trainer eval reward `0.2858995793481605`. These are not enough for selection; queued batch-size-1 reload eval `outputs/gspo_checkpoint_evals/20260521-vibe896-full-eval` is now running.
- External batch-size-1 eval `outputs/gspo_checkpoint_evals/20260521-vibe896-full-eval` completed. The full run was clean but did not beat the shorter canary. Ranking: canary selection `26.3808`, chrF++ `40.9703`, BLEU `8.1555`, token F1 `26.6169`; raw Vibe896 checkpoint selection `25.6181`; full-run checkpoint `56` selection `25.3947`; checkpoint `224`/final selection `25.3835`; checkpoint `168` selection `25.2477`; checkpoint `112` selection `25.2292`. Treat the canary as the current best adapter for the verifier-v2 follow-up.
- Important verifier status: we have tried a learned verifier, but so far it is verifier v1 trained mostly from clean pairs plus synthetic corruptions and hard negatives. The fuller DeepSeek-style next step is verifier v2 trained from real model candidate outputs. Queue script `experiments/gspo/queue_verifier_v2_from_real_candidates.sh` now waits for the current full run and its external checkpoint evals, trains `outputs/chanka_translation_verifier_real_candidates_v2_20260521-verifier-v2-real` using prediction JSONLs from `outputs/gspo_checkpoint_evals/20260521-eosfix-ranking` and `outputs/gspo_checkpoint_evals/20260521-vibe896-full-eval`, then runs a 24-step canary with `learned_verifier_vibe_2511` using the verifier-v2 LoRA. Remote queue log: `outputs/queue_verifier_v2_from_real_candidates_20260521.log`.
- Commit `5679ace` hardened `experiments/gspo/queue_verifier_v2_from_real_candidates.sh`: if verifier-v2 SFT exits nonzero after writing usable checkpoints, the queue now falls back to `final_verifier_lora` when present or otherwise the checkpoint with best logged `eval_loss`. This matters because the previous hard-negative verifier run produced good checkpoints but crashed during final save with CUDA illegal memory access.
- Verifier-v2 real-candidate training started on the remote at `outputs/chanka_translation_verifier_real_candidates_v2_20260521-verifier-v2-real`, PID observed as `127123`. It uses prediction JSONLs from the EOS-fixed ranking plus the full Vibe896 eval. After dedupe it added only 305 real candidate verifier examples, for 8,979 total verifier examples, split into 7,633 train / 1,346 validation. Schedule: 4 epochs, 1,912 total optimizer steps, eval every 59 steps, LoRA r128/a256, LR `2e-5`, batch 4, gradient accumulation 4.
- Verifier-v2 early checkpoints are improving: checkpoint `59` eval loss `0.04840836673974991`, checkpoint `118` `0.0312885046005249`, checkpoint `177` `0.020037654787302017`, checkpoint `236` `0.021803665906190872`, checkpoint `295` `0.017204049974679947`, checkpoint `354` `0.015418198890984058`, checkpoint `413` `0.01523187942802906`, checkpoint `472` `0.015611322596669197`. Preserved best-so-far copies on the remote under `outputs/verifier_selected_checkpoints/`, including current best checkpoint `413`.
- Verifier-v2 later improved to checkpoint `531` eval loss `0.013897795230150223`, checkpoint `590` `0.014394257217645645`, checkpoint `649` `0.013841227628290653`, and checkpoint `708` `0.01347315777093172`. Commit `89fa4e1` adds `experiments/gspo/watch_verifier_best_checkpoint.sh` and expands the queue fallback to search `outputs/verifier_selected_checkpoints/`. The watcher is running on the remote as `outputs/watch_verifier_v2_best_20260521.log` and maintains `outputs/verifier_selected_checkpoints/chanka_translation_verifier_real_candidates_v2_20260521-verifier-v2-real_best`.
- DeepSeek-style verifier-v2 was actually tried. The real-candidate verifier was stopped after overfit signs; best preserved checkpoint was `outputs/verifier_selected_checkpoints/chanka_translation_verifier_real_candidates_v2_20260521-verifier-v2-real_checkpoint-944` with eval loss `0.012264552526175976` (later checkpoints `1003` and `1062` worsened). A 24-step `learned_verifier_vibe_2511` GSPO canary from the current best adapter completed at `outputs/gspo_paper_profiles/2511_verifier_v2_real_candidates_on_best_20260521-verifier-v2-real`. Canonical batch-size-1 eval `outputs/gspo_checkpoint_evals/20260521-verifier-v2-real-verifier-v2-canary` did not beat the previous best: best checkpoint `24`/final selection `25.4932`, chrF++ `40.1668`, BLEU `5.9356`, token F1 `25.6148`, source-copy `2.5105%`, exact-copy `0.6329%`, leakage `0.7911%`, artifact `0.0%`, TER `88.9401`. Previous best remains `learned_verifier_vibe_on_vibe896_4gen_canary` with selection `26.3808`, chrF++ `40.9703`, BLEU `8.1555`, token F1 `26.6169`.
- Commit `382e88a` extends `scripts/evaluate_gspo_checkpoint.py` for verifier candidate mining: `--split train|eval|all`, `--max-train-samples`, `--do-sample`, and `--num-return-sequences`. Use this to build a larger verifier-v3 pool from train/all Chanka rows if verifier-v2 does not beat the current canary. Example pattern: evaluate the current best adapter with `--split train --max-train-samples 512 --do-sample --num-return-sequences 4 --temperature 0.85 --top-p 0.95 --batch-size 1 --predictions-jsonl ...`; then pass that JSONL to `scripts/train_verifier_chanka_unsloth.py --candidate-jsonl`.
- Verifier-v3 sampled-candidate queue script: `experiments/gspo/queue_verifier_v3_sampled_candidates.sh`. It mines sampled candidates from the current best adapter over `--split all` with two temperature/top-p settings by default, trains a verifier with up to 12k real candidate examples, runs a 24-step `learned_verifier_vibe_2511` canary from the current best adapter, then batch-size-1 evaluates checkpoint 8/16/24/final. Launched on the remote with `STAMP=20260521-verifier-v3-sampled`; log `outputs/queue_verifier_v3_sampled_candidates_20260521.log`, candidate dir `outputs/verifier_candidate_mining/20260521-verifier-v3-sampled`, verifier dir `outputs/chanka_translation_verifier_sampled_candidates_v3_20260521-verifier-v3-sampled`, canary dir `outputs/gspo_paper_profiles/2511_verifier_v3_sampled_candidates_on_best_20260521-verifier-v3-sampled`, eval dir `outputs/gspo_checkpoint_evals/20260521-verifier-v3-sampled-verifier-v3-canary`.
- Verifier-v3 completed and improved v2 but did not beat the standing best. Candidate mining produced two 4,220-row sampled sets: `temperature=0.75/top_p=0.90` scored chrF++ `38.5675`, BLEU `8.0288`; `temperature=0.95/top_p=0.95` scored chrF++ `34.2871`, BLEU `5.0552`. Verifier-v3 trained on 7,150 deduped real candidate examples plus synthetic/corruption rows; best verifier eval loss was checkpoint/final `1200` at `0.07788554579019547`. Its best GSPO canary checkpoint was `checkpoint-16` in `outputs/gspo_checkpoint_evals/20260521-verifier-v3-sampled-verifier-v3-canary`: selection `25.7042`, chrF++ `40.5972`, BLEU `5.8257`, token F1 `26.1197`, source-copy `2.6688%`, leakage `0.6329%`, TER `89.6313`. This beats verifier-v2 (`25.4932`) but not current best (`26.3808`).
- `learned_verifier_ensemble_vibe_2511` was added to test v1+v3 verifier ensembling with Vibe diversity. It loads a primary verifier via `--verifier-adapter-path` and a secondary verifier via `--secondary-verifier-adapter-path`. Canary `outputs/gspo_paper_profiles/2511_verifier_ensemble_v1_v3_on_best_20260521-verifier-ensemble-v1-v3` underperformed: best checkpoint `24` selection `25.2932`, chrF++ `40.1285`, BLEU `5.8648`, token F1 `25.1627`, source-copy `2.6688%`, leakage `0.7911%`, TER `89.6313`. Do not full-run this ensemble.
- Low-LR refinement canary script: `experiments/gspo/run_low_lr_refinement_canary.sh`. It continues from the current best adapter using the original `learned_verifier_vibe_2511` reward but with LR `2e-7`, warmup `0`, and `16` max steps. Remote run `outputs/gspo_paper_profiles/2511_low_lr_refine_current_best_20260521-low-lr-refine-current-best` did not beat the standing best: best checkpoint `16` selection `25.9950`, chrF++ `40.3932`, BLEU `8.4617`, token F1 `25.8886`, source-copy `2.6688%`, leakage `0.7911%`, TER `88.9401`. This has the highest BLEU seen so far, but lower chrF++/token-F1 and lower selection than current best.
- BLEU-margin reward profile `learned_verifier_bleu_margin_vibe_2511` and canary script `experiments/gspo/run_bleu_margin_canary.sh` were tested. It keeps the v1 learned verifier but changes the guard term toward chrF++/token-F1 plus a small BLEU/token-precision margin. Remote run `outputs/gspo_paper_profiles/2511_bleu_margin_current_best_20260521-bleu-margin-current-best` underperformed: best checkpoint `8` selection `25.5784`, chrF++ `40.3062`, BLEU `5.8180`, token F1 `25.7731`, source-copy `2.5105%`, leakage `0.6329%`, TER `89.4009`. It did not preserve the high BLEU from the low-LR run and should not be full-run.
- Temperature-refinement sweep script: `experiments/gspo/run_temperature_refinement_sweep.sh`. It tested the original `learned_verifier_vibe_2511` reward from the current best adapter with conservative decoding (`temperature=0.60`, `top_p=0.85`) and exploratory decoding (`temperature=0.90`, `top_p=0.95`), both LR `5e-7`, 16 steps. Eval dir `outputs/gspo_checkpoint_evals/20260521-temp-refine-current-best-temperature-refine-sweep`. Best result was conservative checkpoint `8`: selection `25.9604`, chrF++ `40.5150`, BLEU `8.3218`, token F1 `25.4867`, source-copy `2.6688%`, leakage `0.7911%`, TER `88.4793`. It did not beat standing best selection `26.3808`; simple continuations from the current best are not improving the overall tradeoff.
- Prediction audit after the temperature sweep: failed continuations sometimes fix literal rows but also introduce Spanish leakage, unnecessary stem changes, or paraphrases that lose against the single clean reference. Example regressions include `Señora, ñawpaqman risunchik` for `Adelante, señora`, and variants around `Colegiopim warmayqa...` that lose reference agreement. Next high-upside work should be data/label/reranker improvements rather than more simple current-best continuation sweeps.
- Candidate reranking headroom test on 2026-05-22: `scripts/rerank_candidate_predictions.py` evaluates multi-candidate prediction JSONL files with a reference-aware oracle selector. K=8 sampled eval candidates from the current best adapter reached oracle selection `34.0593`, chrF++ `48.4914`, BLEU `13.9889`, token F1 `36.3623`, source-copy `2.5588%`, exact-copy `0.6329%`, leakage `0.0%`, and TER `76.0369`; the first sampled candidate scored only selection `22.1104`. This proves the current model distribution contains much better outputs than greedy/reload selection, so future high-upside work should train a deployable selector/reranker or preference model rather than only continuing the policy.
- Directly turning that oracle into `reference_rerank_vibe_v1` GSPO did not beat the standing best. The 8-generation canary at `outputs/gspo_paper_profiles/reference_rerank_vibe_current_best_20260522-reference-rerank-current-best` had best external checkpoint `16`: selection `25.7927`, chrF++ `40.3605`, BLEU `8.1634`, token F1 `25.4745`, source-copy `2.6914%`, exact-copy `0.6329%`, leakage `0.7911%`, artifact `0.0%`, TER `89.6313`. Keep current best as `learned_verifier_vibe_on_vibe896_4gen_canary`; the oracle gap is a reranking/selection problem, not solved by this first metric-reward GSPO pass.
- Source-only candidate reranker v1 was tried on 2026-05-22. `scripts/train_candidate_reranker_chanka_unsloth.py` trains a JSON scorer that sees only Spanish source plus candidate Chanka; hidden references create labels. It mined 4,096 train candidates from 512 train rows (`outputs/candidate_reranker_runs/20260522-source-only-reranker-v1/train_candidates_predictions.jsonl`), added clean/source-copy anchors, then trained `outputs/chanka_candidate_reranker_20260522-source-only-reranker-v1`. Held-out K=8 eval with `scripts/evaluate_candidate_reranker.py` failed to improve selection: checkpoint `50` learned selection `22.0813` vs first `22.1104`, and checkpoint `100` selected candidate 0 for every row, exactly matching first selection `22.1104`. The naive source-only JSON scorer is not learning useful candidate ordering; next attempt should be pairwise/listwise training or a smaller discriminative reward model, not more scalar JSON SFT with the same prompt.
- External baselines on 2026-05-22:
  - `scripts/evaluate_translation_baseline.py` can evaluate NLLB, generic seq2seq, and TranslateGemma-style models on the same clean Chanka split.
  - NLLB distilled 600M (`facebook/nllb-200-distilled-600M`, `spa_Latn -> quy_Latn`) completed full held-out eval: chrF++ `24.1377`, BLEU `0.9596`, token F1 `10.1348`, source-copy `6.2395%`, exact-copy `2.5316%`, leakage `1.2342%`, TER `166.1290`. Our current best is far ahead on this Chanka eval.
  - TranslateGemma 4B IT became accessible after HF auth but is not usable for Quechua/Chanka here. A 5-row smoke with `es -> qu` produced Devanagari/Nepali-like outputs and token F1 `0.0`.
  - T5Gemma became accessible after the newer HF token was installed on the remote, but the 5-row smoke remained poor: chrF++ `8.5113`, BLEU `0.3152`, token F1 `0.0`, Spanish leakage `5.3140%`, TER `714.2857`.
- Current external model smoke note: `docs/findings/external_model_baselines.md`. On 2026-05-22, 5-row clean-Chanka smokes were run for `tencent/Hy-MT2-1.8B`, `tencent/Hy-MT2-7B`, `google/gemma-4-E2B-it`, `google/gemma-4-E4B-it`, `google/translategemma-4b-it`, and `google/t5gemma-2-270m-270m`. None were close to the standing internal adapter zero-shot; Gemma 4 E4B produced the cleanest Chanka-like text but was still semantically poor, while Hy-MT2 7B source-copied Spanish under the official prompt style.
- `scripts/evaluate_translation_baseline.py` now supports `causal-chat`, including a Gemma 4 processor path with `enable_thinking=False`. The tokenizer-only Gemma 4 path leaked thinking artifacts.
- `scripts/train_sft_unsloth.py` now allows `--stage chanka` for model-family SFT canaries in addition to the broad data tier.
- Gemma 4 can be trained through Unsloth on the remote: tiny smoke `outputs/smoke_gemma4_e2b_broad_lora2/broad/final_lora` completed with validation enabled and final eval loss `6.6835`. Important caveat: response-only SFT masking must detect Gemma 4 chat markers `"<|turn>user\n"` and `"<|turn>model\n"`; Qwen markers mask out every Gemma row.
- Hy-MT2 can also be trained through Unsloth: `tencent/Hy-MT2-1.8B` and `tencent/Hy-MT2-7B` are recognized as `Hunyuan_V1_Dense`. Tiny 1.8B smoke `outputs/smoke_hymt2_1_8b_chanka_lora2/chanka/final_lora` completed with validation enabled and final eval loss `6.7444`. Tiny 7B user-only prompt smoke `outputs/smoke_hymt2_7b_chanka_hymt2prompt_lora_r32_s128_2steps/chanka/final_lora` completed with validation enabled and final eval loss `6.9954`; a 5-row adapter eval still source-copied Spanish. Important caveat: response-only SFT masking must detect Hy-MT2 1.8B markers `"<｜hy_User｜>"`/`"<｜hy_Assistant｜>"` and Hy-MT2 7B markers `"<|extra_4|>"`/`"<|extra_0|>"`. Use `--prompt-style hymt2` for Hy-MT2 training and baseline eval so train/eval prompts match the model-card protocol.
- Hy-MT2 7B broad-then-Chanka SFT was tested and is not competitive. Broad canary `outputs/hymt2_7b_broad_hymt2prompt_lora_r64_a128_s256_96steps/broad/final_lora` trained on 2,048 broad rows for 96 steps; eval loss improved from `3.446` at step 24 to `2.7638` final. Chanka continuation `outputs/hymt2_7b_broad96_chanka_hymt2prompt_lora_s128_96steps/chanka/final_lora` trained on 512 clean Chanka rows for 96 steps; eval loss improved from `3.025` to `2.6787`. Full held-out deterministic generation scored chrF++ `19.4632`, BLEU `3.1464`, token F1 `4.9005`, source-copy `3.5759%`, leakage `0.3165%`, TER `101.6129`; sampled `temperature=0.1/top_p=0.7/top_k=20` was slightly worse. It now emits Chanka-like text but often wrong semantics, so do not scale Hy-MT2 7B from this tiny recipe unless using substantially more broad data/steps and evaluating early.
- Gemma 4 E4B clean-Chanka SFT was tested but is not competitive. The 32-step canary `outputs/gemma4_e4b_chanka_lora_canary_r64_a128_s128_32steps/chanka/final_lora` scored chrF++ `15.1027`, BLEU `1.0070`, token F1 `4.1937`. The 2-epoch run `outputs/gemma4_e4b_chanka_lora_full_r64_a128_s128_2ep` improved trainer eval loss to `4.2654`, but best external generation among evaluated checkpoints was only checkpoint `392`: chrF++ `16.1325`, BLEU `1.4071`, token F1 `4.1030`, TER `142.8571`. Do not replace Qwen with Gemma 4 based on clean-Chanka-only SFT; if revisiting Gemma/Hy-MT2, try broad Quechua SFT before clean Chanka SFT/GSPO.
- Gemma 4 E4B broad-then-Chanka SFT was also tested and is worse than the clean-only Gemma attempts. Broad canary `outputs/gemma4_e4b_broad_lora_r64_a128_s256_96steps/broad/final_lora` trained on 2,048 broad rows for 96 steps; eval loss improved overall to `4.5090` final but was non-monotonic. Chanka continuation `outputs/gemma4_e4b_broad96_chanka_lora_s128_96steps/chanka/final_lora` trained on 512 clean Chanka rows for 96 steps; eval loss improved to `4.1666`, but full held-out generation scored only chrF++ `14.9705`, BLEU `0.7609`, token F1 `2.4589`, source-copy `3.6920%`, leakage `0.0%`, TER `129.4931`. Failure mode: low Spanish leakage, Chanka-looking fragments, poor semantics. `scripts/evaluate_translation_baseline.py` now supports `--adapter-loader auto|peft|unsloth`; Gemma 4 adapter eval needs the Unsloth loader because PEFT cannot attach LoRA adapters to `Gemma4ClippableLinear`.
- External model availability was refreshed on 2026-05-23. Hy-MT2 has official Hugging Face checkpoints `tencent/Hy-MT2-1.8B`, `tencent/Hy-MT2-7B`, and `tencent/Hy-MT2-30B-A3B`; the 30B-A3B checkpoint is the only one worth revisiting at scale, but Quechua/Chanka is not listed as an official supported language, so treat it as an SFT transfer base. Earlier Gemma 4 E4B tests are negative; revisit Gemma 4 only if a newer official checkpoint/model card materially changes language coverage.
- `scripts/train_sft_unsloth.py --load-in-4bit` exists only for huge non-Qwen model-family smokes. Do not use it for the Qwen3.5 path. `experiments/sft/queue_hymt2_30b_a3b_smoke.sh` uses it for a tiny Hy-MT2 30B-A3B LoRA smoke after the current contextual terminology canary; the script writes `smoke_failed.txt` instead of blocking if the L40S cannot load the model or Unsloth does not support the MoE variant.
- MBR candidate reranking was tested from the MBR papers. `scripts/mbr_candidate_predictions.py` is reference-free and selects the candidate most similar to other sampled candidates, with copy/leakage/artifact guards. On current-best K=8 held-out samples it improved first-candidate selection `22.1104 -> 25.0776`, chrF++ `35.9965 -> 40.1105`, BLEU `6.9190 -> 7.0165`, token F1 `21.2375 -> 23.9195`, source-copy `3.0877% -> 2.1323%`, TER `99.0783 -> 96.7742`. It still trails current greedy best selection `26.3808`, but it is the first deployable reranker that actually helps.
- MBR K/temperature sweep on 2026-05-22: K=16 conservative sampling (`outputs/rerank_candidate_evals/20260522-current-best-k16-t065-p090`, temperature `0.65`, top-p `0.90`) improved MBR over K=8 but still did not beat the standing adapter or terminology-prompt eval. MBR scored selection `25.8597`, chrF++ `40.8152`, BLEU `8.1692`, token F1 `24.1345`, source-copy `1.9673%`, leakage `0.1582%`, TER `93.5484`. The oracle on the same K=16 pool was high: selection `36.7495`, chrF++ `50.8698`, BLEU `18.9527`, token F1 `39.5376`. Tighter K=16 (`outputs/rerank_candidate_evals/20260522-current-best-k16-t050-p085`, temperature `0.50`, top-p `0.85`) had MBR selection `25.7385` and oracle selection `34.9225`. Conclusion: raw model diversity is not the bottleneck; selector/self-training quality is. A custom-data MBR self-training path is the next plausible MBR-paper step, not more K/temperature sweeping.
- MBR self-training v1 was tried on 2026-05-22 and is a negative result. `scripts/train_jsonl_sft_unsloth.py` can SFT from local JSONL rows such as MBR-selected predictions, with validation and pseudo-label filters. Canary `outputs/mbr_self_training_sft/20260522-k8-train512-from-current-best-lr2e-6-64steps` trained from the standing best adapter on 501 filtered MBR pseudo-labels for 64 steps. Best held-out checkpoint was `checkpoint-56`: selection `25.9035`, chrF++ `40.9072`, BLEU `6.1663`, token F1 `26.1799`, source-copy `2.6477%`, leakage `0.6329%`, TER `90.0922`; it did not beat the standing best. Details: `docs/findings/mbr_self_training.md`. Do not scale this exact recipe without stronger pseudo-label confidence, clean-reference anchors, or a better selector/verifier target.
- MBR self-training v2 mixed clean anchors with pseudo-labels using `scripts/build_mixed_sft_jsonl.py`. Mixed canary `outputs/mbr_self_training_sft/20260522-k8-train512-mixed-clean512-from-current-best-lr1e-6-64steps` used 510 clean anchors plus 493 MBR pseudo-labels; best raw held-out checkpoint was `checkpoint-48` with selection `25.9369`, chrF++ `40.5448`, BLEU `8.1424`, token F1 `25.7731`, TER `89.1705`. Terminology-prompt eval on that checkpoint reached BLEU `8.6205` but selection only `26.2287`, below the previous terminology-prompt best selection `26.4615`. Clean anchors prevent BLEU collapse, but this K8 mixed recipe is still not worth scaling without higher-confidence pseudo-labels such as K16/consensus-filtered MBR.
- MBR self-training v3 with confident K16 pseudo-labels produced the first self-training win when paired with terminology prompting. `scripts/build_confident_mbr_pseudo_labels.py` filters K16 MBR choices by score margin, peer utility, copy/leakage, exact-copy, and artifacts. K16 train pool `outputs/verifier_candidate_mining/20260522-train-k16-t065-p090/train_k16_predictions.jsonl` from the standing best adapter has unfiltered MBR selection `27.6782`, chrF++ `42.5243`, BLEU `9.0977`; oracle remains high at selection `36.3301`, chrF++ `51.4576`, BLEU `15.9508`. The margin `0.020` confident subset kept 115 rows with hidden-reference BLEU `13.4162`, then mixed 111 deduped pseudo-labels with 510 clean anchors. Canary `outputs/mbr_self_training_sft/20260522-k16-confident-margin002-clean512-lr8e-7-48steps` was best at `checkpoint-40`: raw selection `26.3578`, chrF++ `41.0006`, BLEU `8.5802`; terminology top-1 eval reached selection `26.4760`, chrF++ `41.0961`, BLEU `8.9872`, token F1 `25.9870`, TER `88.4793`. This is now superseded by the full-train terminology-MBR continuation below. Do not use `final_lora`, which regressed. Details: `docs/findings/mbr_self_training.md`.
- K16 confident margin `0.000` follow-up is a negative result. It mixed 461 pseudo-labels with 510 clean anchors in `outputs/mbr_self_training_sft/20260522-k16-confident-margin000-clean512-lr8e-7-56steps`; best held-out checkpoint/final reached only selection `25.6758`, chrF++ `40.2917`, BLEU `8.5100`, token F1 `25.1140`, leakage `0.7911%`. Do not lower the confidence threshold on this 512-row K16 pool; if expanding this line, generate more K16 candidates and keep high-margin filtering.
- Full-train terminology-MBR continuation was superseded by the next non-terminology candidate-pool continuation. It used `outputs/mbr_self_training_sft/20260522-k16-fullterm-margin000-clean512-lr4e-7-32steps/checkpoint-32` with terminology top-1 inference and scored selection `26.5515`, chrF++ `40.8942`, BLEU `9.4839`, token F1 `26.3462`, TER `88.0184`. Do not use its `final_lora`, which reverted to checkpoint-24 behavior. High-margin filtering on the terminology-mined pool was bad, so do not extrapolate the old margin `0.020` rule to full pools.
- Full-train new-best non-terminology MBR continuation was superseded by terminology-aware JSONL SFT. It used `outputs/mbr_self_training_sft/20260522-k16-fullnewbest-noterm-margin000-clean512-lr3e-7-32steps/checkpoint-24` with terminology top-1 inference and scored selection `26.8011`, chrF++ `41.3017`, BLEU `9.5399`, token F1 `26.6291`, TER `87.5576`. MBR margin `0.005+` was again a bad confidence signal on this pool; broad filter-only MBR plus low-LR continuation and early checkpoint selection remains the better MBR recipe.
- Current deployable best as of 2026-05-22 is the terminology-aware JSONL SFT continuation: `outputs/mbr_self_training_sft/20260522-k16-fullnewbest-noterm-margin000-clean512-termtrain-lr2e-7-24steps/checkpoint-8` with terminology top-1 inference from `clean_chanka/manual_quechua_chanka_glossary_simple_terms.parquet`. `scripts/train_jsonl_sft_unsloth.py` now supports `--terminology-file`, `--terminology-top-k`, and `--terminology-min-source-chars`; this run found 143 matched train rows and 25 matched validation rows in the mixed file. Held-out terminology-prompt eval: selection `27.0396`, chrF++ `41.4823`, BLEU `9.7158`, token F1 `27.0736`, source-copy `2.8270%`, leakage `0.4747%`, TER `87.3272`. The same checkpoint without terminology prompting is weaker: selection `26.5490`, chrF++ `41.1692`, BLEU `9.0400`. Do not use checkpoint-16/24/final from this run; checkpoint-8 is best.
- Raw glossary term-pair augmentation was tested and is negative. `scripts/build_terminology_pair_jsonl.py` builds regenerable Spanish->Chanka term-pair JSONL from the simple glossary; generated artifact `outputs/terminology_augmented_data/20260522-simple-terms/terminology_pairs_target.jsonl` had 128 rows. Canary `outputs/mbr_self_training_sft/20260522-k16-termtrain-best-plus-termpairs-lr1e-7-16steps` started from the current best and mixed the term pairs into the existing MBR+clean data. Best held-out terminology-prompt checkpoint was `checkpoint-8`: selection `26.6310`, chrF++ `41.2503`, BLEU `9.2259`, token F1 `26.2614`, TER `88.2488`, below current best. Trainer eval loss improved only because validation included easy short term pairs. Future terminology augmentation should use glossary-triggered sentence/context examples, not standalone raw term-pair SFT.
- Terminology-matched sentence oversampling was also tested and is negative. `scripts/build_terminology_oversampled_jsonl.py` builds regenerable JSONL that repeats real rows whose source matches glossary terms; `scripts/train_jsonl_sft_unsloth.py` now has `--no-dedupe-rows` for deliberate oversampling. Artifact `outputs/terminology_augmented_data/20260522-oversample-matched/mixed_clean512_confident_margin000_term_oversample_r3.jsonl` repeated 168 matched base rows into 336 extra rows. Canary `outputs/mbr_self_training_sft/20260522-k16-termtrain-best-oversample-r3-lr1e-7-16steps` started from the current best and used repeat ratio 3, LR `1e-7`, 16 steps. Best held-out terminology-prompt checkpoint was `checkpoint-8`: selection `26.3244`, chrF++ `41.2740`, BLEU `6.7682`, token F1 `26.2407`, TER `88.2488`, far below current best. Extra terminology-weighted updates over-steer the model; if revisiting, try much lighter repeat ratios or new full-sentence contextual examples rather than duplicate rows.
- Contextual terminology drills are now implemented via `scripts/build_contextual_terminology_jsonl.py`. It filters the simple glossary and wraps terms in short sentence contexts instead of bare term-pair rows, rejecting stopwords, numeric/code fragments, parenthetical labels, long terms, and multiword outliers. `experiments/sft/queue_contextual_terminology_sft_canary.sh` is queued to wait for the current 9B terminology-SFT chain, then mix these contextual rows with the current clean+MBR JSONL and run a low-LR 16-step canary from the current deployable 2B adapter.
- Pairwise/listwise reranker v1 was tried on 2026-05-22. `scripts/train_pairwise_candidate_reranker_chanka_unsloth.py` trains on hidden-reference pairwise labels, and `scripts/evaluate_pairwise_candidate_reranker.py` tournaments candidate groups. Remote run `outputs/chanka_pairwise_candidate_reranker_20260522-pairwise-v1` trained 7,791 pairwise examples for 100 steps with validation enabled; best eval loss was checkpoint `100` at `0.06099`. Held-out K=8 eval `outputs/pairwise_candidate_reranker_evals/20260522-pairwise-v1-final` parsed 100% of 4,424 pair decisions and selected non-first candidates on 51.90% of rows, but scored only selection `22.3770`, chrF++ `35.4515`, BLEU `8.7623`, token F1 `21.0439`, TER `90.0922`. It improves first-candidate BLEU/TER but is far below MBR selection `25.0776`; keep MBR as the better deployable reranker. Future learned selector work should try discriminative/listwise ranking or train against MBR+oracle features, not this independent JSON pair prompt.
- Oracle-pair DPO was tried on 2026-05-22 and is wired but negative. `scripts/build_oracle_preference_pairs.py` builds `source/chosen/rejected` JSONL from K-candidate pools using hidden training references; `scripts/train_dpo_unsloth.py` runs Unsloth + TRL DPO from an existing LoRA with a frozen reference adapter; `experiments/gspo/run_dpo_oracle_pairs_canary.sh` launches build/train/eval. Best run used 162 margin-0.10 pairs, LR `5e-7`, 8 steps, and terminology top-1 eval: `outputs/dpo_preference_runs/20260522-dpo-oracle-pairs-margin010-lr5e-7/checkpoint-8` scored selection `26.8529`, chrF++ `41.2099`, BLEU `9.5910`, token F1 `26.8049`, source-copy `2.8270%`, leakage `0.4747%`, TER `87.0968`. It improves TER slightly but does not beat the current best selection `27.0396` / chrF++ `41.4823` / BLEU `9.7158`; LR `1e-6` regressed. Plain oracle-pair DPO is not the missing jump; future preference work should be listwise/contrastive, MBR+oracle agreement filtered, or discriminative reranking with features. Details: `docs/findings/preference_training.md`.
- Feature candidate reranking is now the strongest deployable selector path. `scripts/train_feature_candidate_reranker.py` trains a reference-free linear scorer from hidden-reference candidate pools using MBR consensus, length, copy/leakage/artifact, repetition, and candidate-index features. On the fresh current-deployable K16 terminology-prompt held-out pool, matching-pool feature weights trained from `outputs/verifier_candidate_mining/20260522-train-k16-current-deployable-term-t065-p090/train_k16_predictions.jsonl` scored selection `28.5647`, chrF++ `42.9352`, BLEU `11.0872`, token F1 `27.7860`, source-copy `2.0359%`, leakage `0.0%`, TER `84.1014`. This is the current best deployable evaluation by all headline metrics. Remote weights path: `outputs/feature_candidate_reranker_evals/20260522-feature-reranker-train-current-deployable-term-eval-current-deployable-k16/feature_k16_current_term_weights.json`. Details: `docs/findings/candidate_reranking.md`.
- Feature reranker source-root and glossary features were tested and are opt-in only. `--include-source-root-copy` scored selection `28.1603`, chrF++ `42.8616`, BLEU `9.7972`, TER `85.7143`; glossary features with `--include-terminology-features` scored selection `27.8330`, chrF++ `42.4529`, BLEU `8.9455`, TER `84.5622`. Both trail the default feature reranker. Keep default features for deployment; use the experimental features only for diagnostics or after building a richer terminology pool.
- Listwise objectives were added to `scripts/train_feature_candidate_reranker.py` and tested on the mixed K32 pool. Soft listwise scored selection `27.3805`, chrF++ `41.1811`, BLEU `9.0921`, TER `82.9493`; hard-best listwise scored selection `26.8655`, chrF++ `40.5992`, BLEU `8.1170`, TER `81.5668`. Both are below the current K16 feature best and below mixed-pool MBR by selection score. The feature space, not just the objective, is the selector bottleneck.
- Boosted-stump non-linear features were added to `scripts/train_feature_candidate_reranker.py --training-objective boosted-stumps` and tested. Mixed K32 boosted stumps scored selection `27.0303`, chrF++ `41.1587`, BLEU `9.3919`, TER `87.0968`; current K16+soup K8 boosted stumps scored selection `26.8617`, chrF++ `41.1613`, BLEU `7.6396`, TER `88.2488`. Both are negative; shallow feature non-linearity is not enough.
- Reference-free feature-reranked inference wrapper now exists at `scripts/generate_feature_reranked_translations.py`. It loads the adapter, samples K candidates, applies saved feature weights, and writes selected translations without references. Current chrF++/selection/TER best command should use adapter `outputs/mbr_self_training_sft/20260522-k16-fullnewbest-noterm-margin000-clean512-termtrain-lr2e-7-24steps/checkpoint-8`, weights `outputs/feature_candidate_reranker_evals/20260522-feature-reranker-train-current-deployable-term-eval-current-deployable-k16/feature_k16_current_term_weights.json`, K32, temperature `0.65`, top-p `0.90`, top-k `50`, and terminology top-1. K32 transferred K16 weights scored selection `28.6939`, chrF++ `43.3398`, BLEU `9.2082`, token F1 `28.4464`, TER `82.9493`; K16 remains the BLEU-best feature setting with BLEU `11.0872`. Matched K32 feature refit was worse: selection `28.3664`, chrF++ `43.1674`, BLEU `7.3568`, TER `84.3318`. K4 smoke passed at `outputs/manual_inference_smokes/20260522-feature-reranked-smoke`.
- Text-aware hashed reranking is now the best deployable overall profile. `scripts/train_text_candidate_reranker.py` trains an online pairwise logistic selector from oracle winners using hashed candidate n-grams, source-token→candidate-token cross features, and numeric reranker features. `scripts/generate_text_reranked_translations.py` is the deployment wrapper. Same-distribution K32 model `outputs/text_candidate_reranker_evals/20260522-text-ranker-train-k32-eval-k32-term/text_ranker_k32_model.json` scored selection `28.7980`, chrF++ `43.0524`, BLEU `11.9903`, token F1 `28.8624`, TER `82.9493`, source-copy `3.1646%`, leakage `0.0%`. This beats previous K16 feature best on selection/BLEU/token-F1/TER; K32 numeric feature remains the chrF++ best (`43.3398`). Transfer to mixed/soup pools improved BLEU but hurt selection/chrF, so use the text ranker on conservative K32. K4 smoke `outputs/manual_inference_smokes/20260522-text-reranked-smoke` selected `Quinua llaqtapim kawsani` and `45 watayuqmi kani`.
- `scripts/train_text_candidate_reranker.py` now supports `--training-objective listwise`, a softmax objective over full candidate groups using oracle-score soft labels. This directly targets the high-oracle candidate-pool selector gap. Remote ablation at `outputs/text_candidate_reranker_evals/20260523-text-listwise-current-k32-plus-qwen35-4b-full-k16-term` is the new best deployable held-out profile: selection `38.9423`, chrF++ `51.8586`, BLEU `26.0207`, token F1 `40.3340`, source-copy `1.9409%`, leakage `0.0%`, TER `67.7419`. The follow-up score ensemble using this text model was weaker: selection `38.4376`, chrF++ `51.5307`, BLEU `25.3485`, TER `69.1244`. Deploy with `scripts/generate_multi_adapter_ensemble_reranked_translations.py --selection-mode text` and the listwise text model rather than the old ensemble JSON.
- Score-ensemble reranking is now the best deployable profile. `scripts/train_score_ensemble_reranker.py` blends feature score, text score, MBR score/rank, length, duplicate, and candidate-index signals; `scripts/generate_ensemble_reranked_translations.py` is the deployment wrapper. Ensemble `outputs/score_ensemble_reranker_evals/20260522-ensemble-feature-k16-text-k32-eval-k32-term/ensemble_k32_ensemble.json` over feature weights `feature_k16_current_term_weights.json` and text model `text_ranker_k32_model.json` scored selection `29.7584`, chrF++ `43.8064`, BLEU `13.2505`, token F1 `30.4677`, TER `81.3364`, leakage `0.0%`. K4 smoke `outputs/manual_inference_smokes/20260522-ensemble-reranked-smoke` selected `Quinua llaqtapim kawsani` and `Tawa chunka pichqayoq watam kani`.
- Mixed-distribution selector training was tested after the conservative K32 ensemble. Mixed text model `outputs/text_candidate_reranker_evals/20260522-text-ranker-train-mixed-k32-eval-mixed-k32-term/text_ranker_mixed_k32_model.json` scored selection `29.8740`, chrF++ `43.2738`, BLEU `11.7439`, token F1 `30.9609`, TER `81.5668`; this is best selection/token-F1 but trails conservative K32 ensemble on chrF++/BLEU. Mixed ensemble `outputs/score_ensemble_reranker_evals/20260522-ensemble-feature-mixed-text-mixed-eval-mixed-k32-term/ensemble_mixed_k32_ensemble.json` scored selection `29.3974`, chrF++ `42.4715`, BLEU `11.5927`, token F1 `30.4566`, TER `81.1060`; best TER but not best overall. Keep conservative K32 score ensemble as the deployable default.
- Full-finetuning support was added to the SFT scripts via `--training-mode full`, and `scripts/export_unsloth_merged_model.py` can export LoRA adapters to merged 16-bit full models. Direct full SFT from raw `unsloth/Qwen3.5-2B` is technically stable but useless at short horizons: 16-step clean-Chanka full SFT scored chrF++ `10.1459`, BLEU `1.2714`, token F1 `2.3668`, TER `139.8281`. The correct path is merge current best LoRA first: merged model `outputs/merged_full_models/20260522-current-best-checkpoint8-merged16` scored chrF++ `41.7751`, BLEU `7.3813`, token F1 `26.7573`; 16-step full SFT from that merged state scored chrF++ `41.7152`, BLEU `7.3915`, token F1 `26.7486`, TER `89.3983`, effectively neutral. Next full-SFT attempt should use merged current best plus longer horizon/higher LR or JSONL terminology/MBR data, not raw-base full tuning. Details: `docs/findings/full_sft_unsloth.md`.
- Merged-current-best full SFT on the JSONL terminology/MBR mix is also negative. Run `outputs/full_sft_canaries/20260522-current-best-merged-jsonl-term-full-sft-lr2e-6-32step` used the merged current best full model, JSONL `mixed_clean512_confident_margin000_target.jsonl`, terminology top-1 prompts, LR `2e-6`, 32 steps, and validation every 8 steps. Best external checkpoint was `checkpoint-32/final`: selection `26.5312`, chrF++ `41.2908`, BLEU `6.7858`, token F1 `27.2479`, TER `88.2488`, below current best LoRA checkpoint and far below K32 ensemble. K8 candidate check from checkpoint-32 had MBR selection `26.4580`, chrF++ `41.8282`, BLEU `7.3718`, and oracle selection `35.7023` / chrF++ `50.4212` / BLEU `16.3823`, below earlier candidate pools. Do not scale this exact full-SFT JSONL recipe.
- Qwen-family SFT/eval must disable reasoning traces. `scripts/train_gspo_chanka_unsloth.py`, `scripts/train_sft_unsloth.py`, `scripts/train_jsonl_sft_unsloth.py`, and `scripts/evaluate_gspo_checkpoint.py` now call chat templates with `enable_thinking=False` when supported. A Qwen3.5 4B JSONL-only canary initially emitted `Thinking Process:` text and scored chrF++ about `7`; no-thinking inference recovered the same checkpoint to chrF++ `24.1074`, proving the prompt issue. Keep this fix for all Qwen3.5 4B/9B/35B-A3B work.
- Qwen3.5 4B JSONL-only LoRA canary is negative even after the no-thinking fix. Patched run `outputs/model_family_canaries/20260522-qwen35-4b-jsonl-term-lora-r64-lr2e-5-64step-nothink` used `unsloth/Qwen3.5-4B`, the `mixed_clean512_confident_margin000_target.jsonl` target-field data, terminology top-1 prompts, LoRA r64/alpha128, LR `2e-5`, 64 steps, and validation every 16 steps. Best external checkpoint was `checkpoint-48`: selection `11.4808`, chrF++ `23.5161`, BLEU `4.6359`, token F1 `7.9283`, TER `106.4516`; checkpoint-64 had higher chrF++ `23.8631` but worse selection/BLEU. Do not jump directly from raw 4B to the small JSONL self-training mix. Serious 4B/9B work should use the full broad Quechua -> clean Chanka curriculum first, then GSPO/selector work, and only then consider full fine-tuning from a strong merged LoRA.
- Qwen3.5 4B broad -> clean Chanka SFT is now the strongest 4B path. Broad run `outputs/qwen35_4b_curriculum/20260522-broad-r64-a128-s256-512steps/broad/checkpoint-512` reached eval loss `1.387`; Chanka continuation `outputs/qwen35_4b_curriculum/20260522-broad512-chanka-r64-a128-s128-256steps/chanka/checkpoint-224` scored selection `30.3580`, chrF++ `43.2858`, BLEU `16.3507`, token F1 `30.1583`, leakage `0.0%`, TER `85.0230`. It does not beat the K32 ensemble on chrF++ but beats it on BLEU and is a useful candidate generator.
- Qwen3.5 4B merge -> full SFT works on the L40S but is only a tiny refinement. Merged model `outputs/merged_full_models/20260522-qwen35-4b-broad512-chanka224-merged16`; full-SFT canary `outputs/full_sft_canaries/20260522-qwen35-4b-merged-broad512-chanka224-full-chanka-lr1e-6-64steps/chanka/checkpoint-48` scored selection `30.4393`, chrF++ `43.3102`, BLEU `16.2178`, token F1 `30.6209`, TER `85.2535`. Use full SFT only after merging a strong LoRA curriculum checkpoint; raw full SFT and JSONL full SFT are still bad. Details: `docs/findings/qwen35_4b_curriculum_sft.md`.
- Focused 4B full-FT LR sweep recovered a small base-model win. The original `5e-7` run at `outputs/full_sft_sweeps/20260523-qwen35-4b-full-sft-lr-sweep-focus` crashed at checkpoint 48 only because full-model optimizer states filled disk; model checkpoints 12/24/36/48 were evaluable. Best recovered checkpoint is `checkpoint-24`: selection `30.5455`, chrF++ `43.4370`, BLEU `15.3563`, token F1 `31.0685`, TER `84.7926`. This slightly beats the prior 4B full-SFT selection/chrF/token-F1 but loses BLEU. `scripts/train_sft_unsloth.py` and `scripts/train_jsonl_sft_unsloth.py` now default to `--save-only-model` for full fine-tuning. Active follow-up: `outputs/full_sft_sweeps/20260523-qwen35-4b-full-sft-lr-followups`, testing `1e-6` and `2e-6` for 48 steps with model-only checkpoints.
- 4B full-SFT follow-up found a stronger base model. Best standalone checkpoint is now `outputs/full_sft_sweeps/20260523-qwen35-4b-full-sft-lr-followups/lr_2em6_48steps/chanka/checkpoint-36`: selection `31.1999`, chrF++ `44.4295`, BLEU `18.0014`, token F1 `29.9850`, TER `81.5668`. This beats the previous 4B full-SFT and recovered `5e-7` checkpoints on selection/chrF++/BLEU/TER, though not token F1. `scripts/train_sft_unsloth.py` and `scripts/train_jsonl_sft_unsloth.py` now expose `--save-total-limit`; use `--save-total-limit 0` for full-SFT sweeps so model-only checkpoints are not pruned before external eval. Candidate-pool harvest/listwise rerank from this checkpoint is running at `outputs/qwen35_4b_full_sft_candidate_rerank/20260523-qwen35-4b-fft2e6ckpt36-candidate-rerank` with log `outputs/logs/qwen35_4b_fft2e6ckpt36_candidate_rerank_20260523.log`.
- Eval-side K16 sampling from the new 4B FFT checkpoint shows usable selector headroom: first candidate selection `29.1882`, chrF++ `42.5822`, BLEU `16.4238`, TER `81.7972`; oracle selection `41.3831`, chrF++ `54.6467`, BLEU `25.0224`, TER `63.5945`. Train-side K16 generation is still running before the merged-pool listwise text selector can be trained.
- `scripts/train_gspo_chanka_unsloth.py` now has `--attach-lora` for RL from a merged/full checkpoint without accidentally full-parameter RL. `experiments/gspo/queue_qwen35_4b_full_sft_gspo_canary.sh` is queued on the remote behind the active candidate-rerank job: log `outputs/logs/qwen35_4b_fft2e6ckpt36_lora_gspo_20260523.log`, starting base `outputs/full_sft_sweeps/20260523-qwen35-4b-full-sft-lr-followups/lr_2em6_48steps/chanka/checkpoint-36`, reward `learned_verifier_vibe_2511`, LoRA r32/a64, max 16 steps.
- Best held-out profile is now multi-model reranking with the 4B full-SFT checkpoint as a candidate generator and listwise text selection. Current deployable K32 candidates plus Qwen3.5 4B full-SFT K16 candidates, selected by the matching-distribution listwise text reranker, reached selection `38.9423`, chrF++ `51.8586`, BLEU `26.0207`, token F1 `40.3340`, TER `67.7419`; oracle is selection `50.0788`, chrF++ `63.5158`, BLEU `35.7333`. Details: `docs/findings/candidate_reranking.md`.
- Qwen3.5 9B broad -> clean Chanka curriculum finished on the remote at `outputs/qwen35_9b_curriculum/20260522-broad256-chanka192-r64`. Best standalone Chanka checkpoint by external held-out selection is `checkpoint-192` / `final_lora`: selection `28.3241`, chrF++ `41.6747`, BLEU `12.3257`, token F1 `29.2655`, TER `84.7926`. This does not beat the 4B full-SFT checkpoint as a standalone model, but it is strong enough to test as an additional candidate generator.
- 9B eval-only merged-pool diagnostic shows useful diversity but no deployable transfer win yet. Current K32+4B + 9B K16 oracle improved to selection `51.6170`, chrF++ `64.8936`, BLEU `37.1419`, token F1 `56.9913`, TER `48.3871`; however, the existing listwise text selector transferred down to selection `37.6669`, chrF++ `50.6379`, BLEU `24.9145`. Wait for the 9B train pool and train a matched listwise text selector.
- `scripts/write_nested_metrics_summary.py` now repairs nested checkpoint-eval summaries by computing missing `selection_score` values from `metrics.json`. This fixes the 9B queue issue where raw eval metrics lacked a selection score and downstream scripts waited or selected incorrectly. The no-few-shot 9B candidate-rerank queue was restarted on the remote on 2026-05-23 with `checkpoint-192`.
- Retrieval-augmented generation is implemented but not yet scored. `scripts/evaluate_gspo_checkpoint.py` and `scripts/generate_multi_adapter_ensemble_reranked_translations.py` support `--few-shot-top-k`; examples come from the clean train split and exact source matches are excluded to avoid prompt leakage. The 9B candidate-rerank queue has `FEW_SHOT_TOP_K`/`FEW_SHOT_MAX_CANDIDATES` knobs but defaults to `0` for the first scale-only pass. A chained queue `experiments/sft/queue_qwen35_9b_fewshot_candidate_rerank.sh` is running on the remote and waits for the no-few-shot 9B rerank summary before launching a `FEW_SHOT_TOP_K=2` pass. The queue now defaults its text selector to `--training-objective listwise` because the current K32+4B pool showed a large gain from listwise text selection over pairwise text/score-ensemble selection.
- 9B merge -> full SFT is queued but intentionally behind the 9B rerank work. `experiments/sft/queue_qwen35_9b_full_sft_after_rerank.sh` waits for the few-shot rerank summary, selects the best 9B LoRA from the checkpoint eval summary, exports a merged 16-bit model, runs a 2-step terminology-conditioned full-SFT memory smoke, and only then runs a 32-step terminology-conditioned Chanka FFT canary at LR `5e-7`. The script now trains and evaluates with glossary top-1 prompting, avoiding the earlier train/eval prompt mismatch. If it fails on the L40S, keep the recipe for the RTX PRO 6000 rather than concluding full fine-tuning is bad. Current evidence says full FT is not a replacement for the SFT curriculum; it is a late refinement after a strong LoRA checkpoint is merged.
- `scripts/train_sft_unsloth.py` supports terminology-conditioned SFT prompts via `--terminology-file` / `--terminology-top-k`. This aligns Chanka SFT with the terminology-prompted inference profile. `experiments/sft/queue_qwen35_9b_terminology_chanka_sft.sh` is the queued follow-up: after the current 9B rerank/full-SFT chain, it reruns the Chanka LoRA continuation from the 9B broad checkpoint with glossary top-1 prompts and evaluates all checkpoints with the same prompt.
- `experiments/sft/queue_qwen35_9b_checkpoint_soup_eval.sh` is queued as the Deep-Past-style checkpoint averaging follow-up. It waits for the 9B checkpoint eval summary and the few-shot rerank summary, builds a weighted LoRA soup from the top three externally scored 9B Chanka checkpoints, then evaluates the soup with terminology top-1 prompting. This is a low-cost way to test weight averaging after the more important 9B scale/retrieval queues finish.
- Tuned Deep-Past-style MBR consensus selector is implemented in `scripts/train_mbr_consensus_selector.py`. Use `--utility-mode fast`; full sacrebleu pairwise utility is too slow for large candidate pools. On the current K32+4B full-SFT pool, clamped tuned MBR slightly beat default MBR on selection (`29.9682` vs `29.8908`) and TER (`82.7189` vs `84.7926`), but remains far behind the score ensemble (`35.5481`). Keep it as a cheap diagnostic/future ensemble signal, not as the primary selector. Details: `docs/findings/candidate_reranking.md`.
- TranslateGemma paper notes are in `docs/findings/translategemma_takeaways.md`. The off-the-shelf TranslateGemma 4B model was not useful for Chanka in earlier smokes, but the paper's useful ideas are synthetic source selection by sampling-vs-greedy QE, 128-sample QE filtering, prompt consistency, formatting filters, generic instruction-data mixing, and multi-reward RL. Local mixed candidate adaptation (`0.65/0.90` conservative K16 + `0.95/0.95` exploratory K16) showed huge oracle headroom but no deployable gain with current selectors: oracle selection `41.3016`, chrF++ `55.0908`, BLEU `22.6396`, TER `67.2811`; matching mixed-pool feature refit scored only selection `28.3403`, chrF++ `42.0763`, BLEU `9.3382`, TER `85.2535`, below current K16 feature best. Next step should be QE/listwise selection, not linear feature refit.
- User wants a mergekit/frankenstein/MoE experiment for fun after the current mixed-pool reranker work. `scripts/merge_lora_adapters.py` now supports compatible LoRA averaging. First soup `outputs/lora_soups/20260522-termtrain-soup-8-16-24` averaged termtrain checkpoints 8/16/24 with weights `0.50/0.30/0.20`; greedy eval regressed to chrF++ `41.0070`, BLEU `9.1150`, TER `88.9401`, but using it as a K8 candidate generator with current K16 raised oracle from selection `38.3462` / chrF++ `52.6971` / BLEU `19.7691` / TER `69.8157` to selection `39.5617` / chrF++ `54.0888` / BLEU `20.8652` / TER `67.9724`. Current feature weights could not harvest the gain (`27.9722` selection). Next merge work should treat soups/mergekit/MoE as diversity generators and pair them with a stronger selector.
- Kaggle Deep Past Challenge writeups suggest several transferable ideas for this project: glossary-context synthetic drills, slot-fill templates, ambiguity/contrastive drills, MBR/reward-model candidate selection, iterative data repair, conservative pseudo-labeling, and careful checkpoint selection. The most relevant next work is not another plain continuation sweep; it is deployable K16 reranking, glossary-triggered contextual examples, and more reviewed Chanka data. Details: `docs/findings/deep_past_translation_takeaways.md`.
- New papers added locally under `/home/ieqr/Downloads/` (not git): terminology constraints (`2019_training_nmt_apply_terminology_constraints.pdf`, `2020_ijcai_lexical_constraint_aware_nmt_data_augmentation.pdf`), MBR/self-improvement (`2024_chasing_comet_mbr_self_improving_mt.pdf`, `2021_understanding_properties_mbr_decoding_nmt.pdf`), and QE reranking (`2025_qe_reranking_document_translation.pdf`). Actionable takeaways: MBR is worth using now for K-sample inference; terminology constraints suggest appending/packing target Chanka terms into source prompts during SFT/GSPO; scalar QE reranking was not enough, so future selector work should be pairwise/listwise.
- Terminology-conditioned inference was added to `scripts/evaluate_gspo_checkpoint.py`. Use `--terminology-file clean_chanka/manual_quechua_chanka_glossary_simple_terms.parquet` to load the HF-hosted simple glossary and inject matched Spanish->Chanka terms into the prompt. Remote eval dir `outputs/gspo_checkpoint_evals/20260522-terminology-prompt-current-best` tested top-k `1`, `3`, and `6`; all were identical because only 21/158 held-out rows matched terms. This is now the best triage score over the standing adapter: selection `26.4615`, chrF++ `41.0248`, BLEU `8.6118`, token F1 `26.3793`, source-copy `2.8270%`, exact-copy `0.6329%`, leakage `0.4747%`, TER `88.2488`. It fixed `casado/casada` terminology but regressed a few already-good rows; treat it as a useful deployable option and evidence for terminology-aware training augmentation, not as a major model-quality breakthrough.
- Terminology-conditioned GSPO training was added to `scripts/train_gspo_chanka_unsloth.py`, and `experiments/gspo/run_terminology_prompt_canary.sh` runs the canary. Remote run `outputs/gspo_paper_profiles/2511_terminology_prompt_current_best_20260522-terminology-gspo-canary` used the standing best adapter, hard verifier `checkpoint-1368`, LR `3e-7`, `MAX_STEPS=16`, 384 train rows, 64 validation rows, and terminology top-k `1`; validation ran at steps 8 and 16. External reload eval `outputs/gspo_checkpoint_evals/20260522-terminology-gspo-canary-terminology-prompt-canary` did not beat prompt-only terminology inference: best checkpoint/final selection `25.7517`, chrF++ `40.5832`, BLEU `6.3632`, token F1 `25.6711`, leakage `0.6329%`, TER `88.7097`. Do not full-run this setup without broader terminology data; next terminology work should create glossary-triggered synthetic phrase pairs or richer matched examples first.
- Web search on 2026-05-18 found a strong Chanka source pool:
  - MINEDU DSpace subject browse has 109 `Quechua Chanka` items; many records expose CC BY 4.0/open access metadata, but embedded PDF rights text may conflict and must be recorded per source.
  - Best near-term parallel candidates are official gob.pe translated PDF pairs from Indecopi, MINJUS/TTAIP, SUNASS, CONTIGO, and ANA.
  - ANA water law is explicitly bilingual Spanish/Quechua Chanka but metadata indicates CC BY-NC-ND 4.0, so keep separate from any permissive release unless the team accepts the restriction.
  - IWSLT 2026/2025 Quechua-Spanish data is useful for baselines but mixes Chanka (`quy`) and Collao (`quz`) and is licensed CC BY-NC-ND 3.0.
  - Mozilla Common Voice Ayacucho Quechua (`quy`) is CC-0 and useful for ASR/text prompts, not Spanish parallel data.
- Some public Quechua resources may have unclear licenses, domain mismatch, orthography differences, or mixed language varieties.
- Licensing risk is currently a primary reason to narrow scope to Chanka Quechua.
- The current PDF extraction uses PyMuPDF table detection, not OCR. Visual checks were done on ambiguous rendered pages. Rows with context notes, alternatives, filled merged cells, or extraction caveats still need review before use for training.
- Translation quality for low-resource languages can look deceptively good in aggregate metrics while failing cultural, dialectal, or terminology-sensitive cases.
- Do not collapse all Indigenous languages or all Quechua varieties into one undifferentiated target.

## Useful Next Steps

- Fill `README.md` with the project statement, setup instructions, and first milestone.
- Identify target translation directions and Quechua variety/varieties.
- Inventory available datasets and licenses.
- Choose an initial baseline model and evaluation strategy.
- Decide whether the hackathon deliverable is a model, dataset, demo, paper-style report, or a combination.
- Add reproducible scripts for data preparation, training/post-training, evaluation, and demo inference as the project grows.

## 2026-05-23 Latest Base-Model Notes

- Best standalone 4B full-SFT base so far: `outputs/full_sft_sweeps/20260523-qwen35-4b-full-sft-lr-followups/lr_2em6_48steps/chanka/checkpoint-36`, selection `31.1999`, chrF++ `44.4295`, BLEU `18.0014`, token F1 `29.9850`, TER `81.5668`.
- Current best deployable held-out profile is still current K32 plus old Qwen3.5 4B full-SFT K16 with the matching listwise text selector: selection `38.9423`, chrF++ `51.8586`, BLEU `26.0207`, token F1 `40.3340`, TER `67.7419`.
- Adding the newer 4B full-SFT checkpoint as another K16 candidate generator did not improve deployment yet: matched listwise text scored selection `38.3355`, chrF++ `51.6498`, BLEU `23.9115`, token F1 `39.6077`, TER `69.1244`, despite oracle `51.1497` / chrF++ `64.5949` / BLEU `37.1959`.
- GSPO from a full-SFT checkpoint should use a fresh LoRA adapter, not full-parameter RL on the L40S. `experiments/gspo/queue_qwen35_4b_full_sft_gspo_canary.sh` now defaults train/eval batch size to `4` for `NUM_GENERATIONS=4`; earlier failures were only Unsloth GRPO batch-divisibility guard failures.
- LoRA-GSPO from the best 4B full-SFT checkpoint completed but was negative: best checkpoint/final scored selection `30.9849`, chrF++ `44.3754`, BLEU `18.1300`, token F1 `29.2240`, TER `81.7972`. It nudged BLEU over the full-SFT base but lost on selection/token F1; all trainer eval completions hit the 80-token cap, so fix termination/length reward before scaling this recipe.
- 9B full-SFT requires `paged_adamw_8bit` on the L40S. Regular `adamw_8bit` OOMed after merging the 9B LoRA checkpoint to 16-bit; a 2-step paged optimizer smoke completed.
- 9B paged full-SFT canary from `outputs/qwen35_9b_curriculum/20260522-broad256-chanka192-r64/chanka/chanka/checkpoint-192` is mechanically viable but not competitive: best final model scored selection `28.4456`, chrF++ `41.9470`, BLEU `13.0248`, token F1 `28.8405`, TER `81.3364`. Keep `--optim paged_adamw_8bit` for large-model probes, but do not scale this exact recipe.
- Remote disk on `root@216.81.248.197 -p 20299` was cleaned after the 9B canary: full 9B canary model artifacts and the temporary merged 9B model were removed, while `checkpoint_eval` metrics/predictions were kept. Disk recovered from about 8.8 GB free to about 62 GB free.
- Contextual terminology SFT canary `outputs/contextual_terminology_sft/20260523-contextual-terminology-sft-now` generated 192 contextual glossary rows and mixed them into the current clean+MBR JSONL, but it was negative. Best checkpoint `checkpoint-8` scored selection `26.3522`, chrF++ `41.3141`, BLEU `6.7073`, token F1 `26.4407`, TER `88.0184`, below the current deployable 2B adapter. Do not scale this exact synthetic terminology drill recipe.
- `scripts/train_jsonl_sft_unsloth.py` now defines the same hidden `--load-in-4bit` flag expected by the shared training-mode validator and passes load flags consistently. This fixes JSONL continuation runs that start from local adapters.
- Hy-MT2 30B-A3B Unsloth smoke `outputs/hymt2_30b_a3b_canaries/20260523-hymt2-30b-a3b-smoke` failed before downloading weights because the checkpoint uses `model_type: hy_v3`, which `transformers==5.5.0`/Unsloth `2026.5.5` does not recognize. This is a compatibility blocker, not a quality result. The model inventory is about 60.15 GB, so future retries need both `hy_v3` support and enough download space.
- Gemma 4 26B-A4B Unsloth smoke `outputs/gemma4_26b_a4b_canaries/20260523-gemma4-26b-a4b-smoke` passed the architecture gate but failed during 4-bit loading on the L40S because modules would be dispatched to CPU/disk. This is a 44 GB GPU memory blocker, not a quality result. The model inventory is about 51.64 GB; the temporary 49 GB cache was deleted after the failed smoke.
- Qwen3.5 35B-A3B Unsloth smoke `outputs/qwen35_35b_a3b_canaries/20260523-qwen35-35b-a3b-smoke` passed the architecture gate (`Fast Qwen3_5_MoE`) but failed during 4-bit loading on the L40S because modules would be dispatched to CPU/disk. This is a 44 GB GPU memory blocker, not a quality result. The temporary 67 GB cache was deleted after the failed smoke and `/root` returned to about 120 GB free. Retry only on the RTX PRO 6000 or with explicit offload/device-map work.
- 4B full-SFT GSPO termination follow-up is negative. `scripts/train_gspo_chanka_unsloth.py` now has optional overlong-completion reward guards and explicit GRPO `generation_kwargs` for EOS/PAD; `experiments/gspo/queue_qwen35_4b_full_sft_gspo_canary.sh` exposes max prompt/completion and overlong knobs. Reward-only guard run `20260523-qwen35-4b-full-sft-lora-gspo-overlong-guard` still clipped every trainer sample at 80 tokens and scored selection `29.6703`, chrF++ `43.0678`, BLEU `14.8357`, TER `86.4055`, below the 4B full-SFT base. Explicit EOS/PAD did not fix clipping. A `MAX_COMPLETION_LENGTH=32` smoke still clipped every sample but was faster and slightly better: selection `29.7981`, chrF++ `43.1093`, BLEU `14.9865`, TER `85.7143`. Future GSPO canaries should use shorter RL completions for speed, but do not scale this GSPO path until it beats the base or sampler termination is actually fixed.
- Candidate provenance plumbing was added on 2026-05-23. Candidate loaders/writers now preserve optional `pool_path`, generation wrappers set it to the adapter path, and feature/text rerankers can opt into pool-origin features. The simple origin features are negative on the mixed 4B+9B selector: origin+token text scored selection `36.1156`, chrF++ `49.2064`, BLEU `21.9106`; origin-only scored selection `35.7782`, chrF++ `48.9125`, BLEU `21.7656`; current no-origin retrain control scored selection `33.7561`, chrF++ `46.2980`, BLEU `21.8392`. All trail the previous matched listwise text selector (`38.1557`, chrF++ `51.2895`, BLEU `23.4968`). Old saved selector models remain loadable, but do not replace a saved best selector with a freshly retrained one without external eval.
- Active training launched on 2026-05-23: 4B full-SFT low-LR refinement from `outputs/full_sft_sweeps/20260523-qwen35-4b-full-sft-lr-followups/lr_2em6_48steps/chanka/checkpoint-36`. Remote host `root@216.81.248.197 -p 20299`, launch PID `254147`, log `outputs/full_sft_sweeps/20260523-qwen35-4b-full-sft-refine-from-ckpt36-lr5e-7-72steps/run.log`. Config: full fine-tune, `paged_adamw_8bit`, LR `5e-7`, 72 steps, eval/save every 8 steps, terminology top-1 prompts, 897 train / 158 validation rows, all 4.54B parameters trainable. After training, the wrapper evaluates kept checkpoints with `scripts/evaluate_gspo_checkpoint.py` and writes a nested metrics summary.
- DeepSeekMath-style bounded-thinking v3 with meta-verifier-v3 did not improve translation quality. Run `outputs/gspo_paper_profiles/2511_self_verifiable_thinking_translation_meta_v3_20260523-meta-v3-thinking/chanka_gspo/final_metrics.json` scored chrF++ `32.5425`, BLEU `8.5679`, token F1 `17.9425`, TER `119.6532`, trainer eval reward `0.0970`, format/thinking-format `50%`, false-confidence `93.75%`. This is worse than the earlier bounded-thinking v2 canary and far below the 4B full-SFT base. Treat it as evidence that self-verification syntax and meta-verifier reward alone are not enough; the next branch should use SFT cold-started primitive thinking before GSPO.
- Fast GSPO canary controls were added to `scripts/train_gspo_chanka_unsloth.py`: `--no-trainer-eval`, `--final-metrics-max-samples`, and `--final-generation-batch-size`. For slow structured-output experiments, use trainer eval only on serious runs; for quick head-start canaries prefer final metrics on 16-32 rows with generation batch 8. `queue_2511_thinking_generator_sft_after_active.sh` now defaults to this faster path after the SFT cold start.
